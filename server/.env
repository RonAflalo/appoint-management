PORT=3001
JWT_SECRET=appointment-super-secret-key-change-in-production
CLIENT_URL=http://localhost:5173

# Email (Brevo SMTP — free 300/day, works for all recipients)
# Sign up at https://app.brevo.com → SMTP & API → SMTP tab → Generate SMTP key
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=your-brevo-login-email@example.com
SMTP_PASS=your-brevo-smtp-key
EMAIL_FROM=תוריי <your-brevo-login-email@example.com>
